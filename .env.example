# ============================================
# NEXUS Music Manager - Environment Variables
# ============================================
# Copy this file to .env and fill in your API credentials
# DO NOT commit .env to git (already in .gitignore)

# ============================================
# YouTube Data API v3
# ============================================
# Get your key: https://console.cloud.google.com/apis/credentials
# Enable: YouTube Data API v3
# Required for: Search & Download tab (YouTube search)
YOUTUBE_API_KEY=your_youtube_api_key_here

# ============================================
# Spotify Web API
# ============================================
# Get your credentials: https://developer.spotify.com/dashboard/applications
# Create app -> Get Client ID and Client Secret
# Required for: Search & Download tab (Spotify search)
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here

# ============================================
# Optional Settings
# ============================================
# Database location (default: ./database/music_library.db)
# DB_PATH=./database/music_library.db

# Download folder (default: ./downloads)
# DOWNLOAD_FOLDER=./downloads

# Maximum concurrent downloads (default: 50)
# MAX_CONCURRENT_DOWNLOADS=50

# Enable debug logging (default: false)
# DEBUG=false

# ============================================
# Notes
# ============================================
# 1. This app currently uses OS keyring for credential storage
#    (via keyring library) instead of .env files
# 2. To use .env instead, modify src/gui/tabs/search_tab.py
#    to load from dotenv instead of keyring
# 3. The .env file is already ignored in .gitignore
# 4. For production, use OS keyring (more secure than .env)

# ============================================
# Security Best Practices
# ============================================
# - Never commit .env file to version control
# - Use different API keys for development/production
# - Rotate API keys regularly (every 3-6 months)
# - Monitor API quota usage in cloud consoles
# - Keep .env file permissions restricted (chmod 600)
